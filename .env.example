# Local Docker tests (app + postgres in one compose network).
POSTGRES_DB=netden
POSTGRES_USER=netden
POSTGRES_PASSWORD=netdenpass
POSTGRES_PORT=5432
DATABASE_URL=postgresql://netden:netdenpass@postgres:5432/netden?schema=public
NETDEN_SESSION_SECRET=local-session-secret-change-me
NEXT_PUBLIC_BUILD_SHA=dev
PROXY_PORT=8080

# Timeweb deploy flow is postponed in this iteration.
# For first deploy setup mode keep values empty:
# DATABASE_URL=
# NETDEN_SESSION_SECRET=
#
# After /setup copy generated values and redeploy:
# DATABASE_URL=postgresql://APP_ROLE:APP_PASSWORD@HOST:5432/DB_NAME?schema=public&sslmode=require
# NETDEN_SESSION_SECRET=generated-in-setup-summary
